# elasticsearch-indices-plugin

Simple tool to read metrics for every index on a ElasticSearch, then output InfluxDB line protocol.

This tool is meant to be used with Telegraf's `inputs.exec` plugin.